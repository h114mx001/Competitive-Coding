#include <bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(0); // add this line for more efficient I/O
    cin.tie(0);              // and this
    freopen("task.inp","r",stdin);
    freopen("task.out","w",stdout);
    // int a, b;
    // string animal;
    // cin >> a >> b >> animal;
    string s;
    getline(cin, s);
    // cout << a << " " << b << " " << animal << "\n"; //using /n instead of endl;
    cout << s << "\n";
}